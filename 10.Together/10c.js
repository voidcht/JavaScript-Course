function execute(){
  const buttonElement = document.querySelector('.js-button');
  if(buttonElement.classList.contains('js-button')){
    console.log(`The class of the button is 'js-button'.`);
  }
}